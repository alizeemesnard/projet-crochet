{% extends 'base.html.twig' %}

{% block title %}CrochetPattern{% endblock %}

{% block body %}
    <h1>CrochetPattern</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ crochet_pattern.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ crochet_pattern.Name }}</td>
            </tr>
            <tr>
                <th>HookSize</th>
                <td>{{ crochet_pattern.HookSize }}</td>
            </tr>
            <tr>
                <th>Category</th>
                <td>{{ crochet_pattern.Category }}</td>
            </tr>
            <tr>
                <th>Language</th>
                <td>{{ crochet_pattern.Language }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ crochet_pattern.Image ? crochet_pattern.Image|join(', ') : '' }}</td>
            </tr>
            <tr>
                <th>Designer</th>
                <td>{{ crochet_pattern.Designer }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_crochet_pattern_index') }}">back to list</a>

    <a href="{{ path('app_crochet_pattern_edit', {'id': crochet_pattern.id}) }}">edit</a>

    {{ include('crochet_pattern/_delete_form.html.twig') }}
{% endblock %}
